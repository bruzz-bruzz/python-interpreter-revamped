# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# NOTE: `dist/` and bare `lib/` are not ignored at the top level
# because the new `web/` frontend has its own `dist/` (Vite build)
# and `web/src/lib/` (TS source dir). Those are scoped below.

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/

# Logs
*.log

# Node / frontend
web/node_modules/
web/dist/
web/dist-node/
web/*.tsbuildinfo

